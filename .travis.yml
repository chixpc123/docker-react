sudo: required
services:
  - docker

before_install:
  - docker build -t chixdocker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true chixdocker/docker-react npm run test -- --coverage
